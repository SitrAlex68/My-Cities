import React from "react";
import {
    View,
  } from 'react-native';

export default class WhiteButton extends React.Component{
    constructor(props){
        super(props);
    }

    render(){
        const {val} = this.props;
        return (
            <View>
            </View>
        )
    }
}